{
    "name": "mdify",
    "version": "1.0.0",
    "description": "Convert text to markdown using AI",
    "scripts": {
        "frontend:install": "cd frontend && npm install",
        "frontend:build": "cd frontend && npm run build",
        "build": "npm run frontend:build",
        "dev": "netlify dev"
    },
    "dependencies": {
        "axios": "^1.13.4"
    },
    "devDependencies": {
        "netlify-cli": "^17.38.0"
    }
}